# Normalize line endings. Shell scripts and git hooks MUST stay LF so they run
# under sh (Git Bash on Windows, Linux, macOS) — CRLF breaks the interpreter.
* text=auto
.githooks/** text eol=lf
*.sh text eol=lf
